int main(){
    // Check if button is pressed
    // If pressed then run little engine to open door.
        // If pressed, keep a wait of 3 seconds, so the button can not be spammed.
            // Need to check if door is opened? Possible extension.
    // Else just wait.
}